{
  "name": "fit-scout",
  "private": true,
  "packageManager": "pnpm@10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -C apps/api dev & pnpm -C apps/web dev",
    "build": "pnpm -C apps/api build && pnpm -C apps/web build",
    "start": "pnpm -C apps/api start & pnpm -C apps/web start",
    "embed": "pnpm -C packages/ai embed",
    "lint": "echo 'add eslint later'",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "vitest": "^3.2.4"
  }
}
